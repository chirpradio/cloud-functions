{
  "name": "process-playlist-track",
  "version": "1.0.0",
  "description": "Add images and populate related data for other functions",
  "main": "index.js",
  "scripts": {
    "start": "yaml2json env.yaml >.env.json && ./node_modules/.bin/env-cmd -f ./.env.json functions-framework --target=processPlaylistTrack --port=8081 --signature-type=event",
    "deploy:dev": "gcloud functions deploy processPlaylistTrack --project chirpradiodev --runtime=nodejs16 --env-vars-file env.yaml --trigger-topic playlist-event",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/datastore": "^6.6.2",
    "@google-cloud/pubsub": "^2.19.0",
    "lastfm-node-client": "^2.2.0"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.1.0",
    "env-cmd": "^10.1.0",
    "jest": "^27.5.1",
    "yaml2json": "^1.0.2"
  }
}
